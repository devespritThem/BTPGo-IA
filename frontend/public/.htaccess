Options -Indexes -MultiViews

# Serve index.html for any non-file/non-directory request
# This enables deep links like /login or /demo to work in production
# while still allowing static assets to be served normally.
FallbackResource /index.html

