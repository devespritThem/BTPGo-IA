# Création du fichier localement
echo "# 🌐 URL du backend (API principale)
VITE_BACKEND_URL=http://localhost:4000

# ⚙️ Configuration serveur backend
PORT=4000
NODE_ENV=development

# 🔒 Base de données
DATABASE_URL=postgresql://user:password@localhost:5432/btpgo_db

# 🧠 Clés IA / API externes
OPENAI_API_KEY=sk-your-openai-key-here
ANTHROPIC_API_KEY=your-anthropic-key-here

# 🔐 Authentification
JWT_SECRET=your-secret-jwt-key
SESSION_SECRET=your-session-secret

# 💬 Services externes (facultatifs)
EMAIL_SERVER=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=example@gmail.com
EMAIL_PASSWORD=yourpassword

# 🌍 Frontend (si nécessaire pour les redirections)
VITE_APP_URL=http://localhost:5173
" > .env.example

# Ajout au dépôt
git add .env.example
git commit -m "Add .env.example for environment configuration"
git push origin main
